// ============================================================================
//  НАСТРОЙКИ МОДЕЛИ И СТАВОК — единственное место, где стоит менять числа.
//  Меняйте только значения справа от двоеточия. Проценты записаны долями:
//  0.15 = 15%, 0.05 = 5%. После правки запустите `npm test`, затем
//  `npm run update` — и сравните метрики на сайте с тем, что было до правки.
//
//  ВАЖНО: подкручивать числа, глядя на результат бэктеста, — значит подгонять
//  модель под прошлое. Честная проверка правки — только живая проверка на
//  новых матчах (раздел «Живая проверка прогнозов» на сайте).
// ============================================================================

export const SETTINGS = {
  model: {
    // Шаг рейтинга Elo: насколько сильно один матч двигает рейтинг.
    // Больше — рейтинг быстрее реагирует на форму, но сильнее «шумит». Проверено: 40.
    eloK: 40,
    // Стартовая неопределённость рейтинга Glicko-2 для новой команды (RD).
    // Больше — модель дольше не доверяет новичкам. Проверено: 350.
    glickoStartRd: 350,
    // Как быстро растёт неопределённость, пока команда не играет:
    // один «период» простоя в днях. Меньше — быстрее забывает старую силу.
    glickoPeriodDays: 7,
    // Изменчивость силы команды в Glicko-2. На наших данных почти не влияет.
    glickoTau: 0.5,
    // Сколько последних матчей считается «формой» команды.
    formWindow: 10,
    // Потолок паузы между матчами в днях: дольше этого пауза считается одинаковой.
    restCapDays: 60,
    // Какая доля самых поздних матчей откладывается для честной проверки
    // и не участвует в обучении весов регрессии. 0.2 = последние 20%.
    testShare: 0.2,
  },

  betting: {
    // Стартовый банк в калькуляторе Келли на сайте, ₽.
    bank: 2000,
    // Доля от ставки по Келли. 1 = полный Келли (агрессивно), 0.25 = четверть.
    kellyFraction: 0.25,
    // Максимум на одну ставку, доля банка.
    maxStake: 0.05,
    // Максимум на все одновременные ставки, доля банка.
    maxTotal: 0.3,
    // Если EV выше этого порога, строка помечается ⚠: скорее всего, модель
    // чего-то не знает. Такие ставки по умолчанию пропускаются.
    flagEv: 0.15,
    // За сколько минут до начала матча принимается решение в бэктесте ставок.
    leadMinutes: 15,
    // Самая старая котировка, которой ещё можно пользоваться, в минутах.
    maxQuoteAgeMinutes: 60,
    // Консенсус букмекеров: ставка, если лучший коэффициент выше
    // 1 / (средняя вероятность − alpha). 0.05 — значение из статьи.
    consensusAlpha: 0.05,
  },

  update: {
    // `npm run update`: за сколько последних дней догружать результаты.
    resultsDays: 3,
    // На сколько дней вперёд забирать ближайшие матчи.
    aheadDays: 7,
    // Сколько запросов статистики игроков делать за одно обновление.
    statsLimit: 300,
    // Сколько часов вперёд показывать ближайшие матчи на сайте.
    boardHours: 72,
  },
};
